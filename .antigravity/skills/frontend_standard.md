# SKILL: Estándar de Programación 💻
- **Arquitectura**: Componentes funcionales en React con TypeScript estricto.
- **Estructura**: `src/components`, `src/hooks`, `src/services`, `src/types`.
- **Protección**: Aplicar `[NULL_SAFETY]`. Si una propiedad de la API falla, el componente debe renderizar un estado seguro o `null`, nunca romper la app.